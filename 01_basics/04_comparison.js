console.log("2">1);
//before comparing you should always ensure the datatypes of the both should be the same
console.log(null==0);//Rule: null is only equal to undefined, not to numbers.
console.log(null>=0);
console.log(null<=0);
console.log(null<0);
//in js equality and comparision are different both works in different ways
console.log(undefined<0);
console.log(undefined==0);
console.log(undefined<=0);

//=== ->triple equator it checks strictly it not only checks the values but also checks the datatype
console.log("2"==2);
console.log("2"===2);

